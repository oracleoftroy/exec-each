{
	"name": "exec-each",
	"version": "0.0.2",
	"description": "Run a command for each file in a glob expansion",
	"bin": "src/main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "MIT",
	"dependencies": {
		"glob": "^7.1.6",
		"yargs": "^14.2.2"
	}
}
